export function ErrorMessages(name) {
    const messages = {
        undefined: `${name} is not defined`
    };
    if (name) {
        return messages;
    }
}
//# sourceMappingURL=error-messages.func.js.map